import React from 'react'
import { Button, Input } from 'components';

const Login = () => {
    return(
        <div>
            <Input/>
            <Button/>
        </div>
    )
}

export default Login;